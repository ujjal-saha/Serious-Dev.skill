# Serious-Dev.skill
Library First is an AI agent skill that makes coding agents stop and ask "can a library do this?" before writing code. It checks a built-in catalog of 194 trusted packages for Python, JavaScript, C++, Java, Go, Rust, C# and more, falls back to a quick web search, then installs and uses the package. Less code, fewer bugs, safer installs.
